# Lesson: Cross-Compilation

## Learning Objectives
- Use `GOOS` and `GOARCH` for cross-compilation
- Understand CGO_ENABLED and its impact
- Build binaries for multiple platforms from a single host

## Exercise
Create a program that demonstrates cross-compilation to different target platforms.