# Lesson: Release Metadata

## Learning Objectives
- Embed version information into binaries at build time
- Use `-ldflags` to set build variables
- Implement version flags in CLI applications

## Exercise
Create a program that embeds version, commit, and build time information into the binary.