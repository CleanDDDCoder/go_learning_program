# Context Boundaries

This lesson teaches you how to:
- Use `context.Context` for request-scoped values
- Implement cancellation with contexts
- Set deadlines and timeouts
- Propagate context through function calls

## Exercises

1. Create a function that accepts a context and implements cancellation
2. Use context to pass request-scoped values through a call stack